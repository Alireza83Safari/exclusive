export type getBrand = {
  code: string;
  createdAt: string;
  fileId: string;
  fileName: string;
  fileType: number;
  fileUrl: string;
  id: string;
  name: string;
  updatedAt: string;
};

export type brand = {
  code: string;
  name: string;
};

export type brandSelectListType = {
  key: string;
  value: string;
};
