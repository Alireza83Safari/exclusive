import TopHeader from "./Header/TopHeader";
import MainHeader from "./Header/MainHeader";
function Header() {
  return (
    <header>
      <TopHeader />
      <MainHeader />
    </header>
  );
}

export default Header;
